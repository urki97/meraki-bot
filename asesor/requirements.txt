# SDK de Anthropic — sin fijar versión a propósito: el parámetro `fallbacks`
# (beta jun-2026) necesita una versión reciente. Si ves TypeError en
# messages.create, haz `pip install -U anthropic` / rebuild.
anthropic
python-telegram-bot>=21.6
APScheduler>=3.10,<4
faster-whisper>=1.0
python-dotenv>=1.0
tzdata
